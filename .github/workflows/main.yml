name: Full Deploy - Node.js + Firebase Hosting

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🔹 سحب المشروع من GitHub
        uses: actions/checkout@v3

      - name: 🔹 تثبيت Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 🔹 التحقق من وجود npm
        run: npm -v

      - name: 🔹 تثبيت الحزم المطلوبة (npm install)
        run: npm install

      - name: 🔹 بناء المشروع (npm run build)
        run: npm run build

      - name: 🔹 تثبيت أدوات Firebase CLI
        run: npm install -g firebase-tools

      - name: 🔹 التحقق من نسخة Firebase
        run: firebase --version

      - name: 🔹 نشر المشروع إلى Firebase Hosting
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: firebase deploy --only hosting
